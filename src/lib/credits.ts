/**
 * Barème des crédits.
 *
 * Un seul endroit pour ces valeurs : elles seront un jour éditables depuis
 * l'administration, et il ne faut pas avoir à les chercher dans six fichiers.
 */

/** Crédits débités à l'envoi d'un message. */
export const COUT_MESSAGE = 1;

/** Crédits offerts à l'inscription, pour que le premier échange soit possible. */
export const BONUS_BIENVENUE = 20;

/** Minutes de vidéo, pour plus tard. */
export const COUT_MINUTE_VIDEO = 4;
